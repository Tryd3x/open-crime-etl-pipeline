CREATE TABLE IF NOT EXISTS date (
    date DATE PRIMARY KEY,
    day INTEGER,
    month INTEGER,
    month_name VARCHAR,
    year INTEGER,
    day_of_week INTEGER,
    day_of_week_name VARCHAR,
    holiday_name VARCHAR
)